#include<stdio.h>
void main()
{
        int r,rev=0,num;
        printf("Enter a number :");
        scanf("%d",&num);
        while(num)
        {
                r=num%10;
                rev=rev*10+r;
                num/=10;
        }
        printf("Reverse number is %d\n",rev);
}
