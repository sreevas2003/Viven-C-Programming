#include<stdio.h>
void main()
{
        int i,n,sum=0;
        printf("Enter N value : ");
        scanf("%d",&n);
        for(i=1;i<=n;i++)
        {
                if(i%2!=0)
                        sum+=i;
        }
        printf("Odd sum of 1 to %d is %d\n",n,sum);
}
