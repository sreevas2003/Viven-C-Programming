#include<stdio.h>
void main()
{
        char s[15];
        printf("Enter a string : ");
        fgets(s,15,stdin);
        int i=0;
        while(s[i]!='\0')
        {
                if(s[i]>96 && s[i]<123)
                        s[i]-=32;
                i++;
        }
        puts(s);
}
