#include<stdio.h>
void main()
{
        int i,num;
        printf("Table number : ");
        scanf("%d",&num);
        for(i=1;i<=10;i++)
        {
                printf("%3d * %3d = %3d\n",num,i,num*i);
        }
}
