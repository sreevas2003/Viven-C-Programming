#include<stdio.h>
void main()
{
        int i,max;
        int a[9]={2,4,6,8,9,7,5,3,1};
        max=a[0];
        for(i=1;i<9;i++)
        {
                if(max<a[i])
                      max=a[i];
        }
        printf("In array maximum = %d\n",max);
}
