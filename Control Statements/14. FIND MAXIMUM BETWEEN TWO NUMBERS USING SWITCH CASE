#include<stdio.h>
void main()
{
        int a,b;
        printf("Enter any Two Numbers : ");
        scanf("%d%d",&a,&b);
        switch(a>b)
        {
                case 0: printf("%d is maximum\n",b);
                        break;
                case 1: printf("%d is maximun\n",a);
        }
}
