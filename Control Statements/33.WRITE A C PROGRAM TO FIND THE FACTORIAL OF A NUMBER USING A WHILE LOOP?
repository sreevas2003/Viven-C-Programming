#include<stdio.h>
void main()
{
        int i=1,fact=1,n;
        printf("Enter a number : ");
        scanf("%d",&n);
        while(i<=n)
        {
                fact*=i;
                i++;
        }
        printf("factorial of %d is %d\n",n,fact);
}
